# apbd-cw1-git-s32756
s32756 apbd assignment
1- If a new branch has commits and pushes, however the main branch does not have any new commits/pushes and if we merge the main branch with newly created branch it is called FAST-FORWARD merge.
2- Branch is created to improve/add new functionalities to our program. In the end, this newly created branch is to be merged to the main branch. A lot of branches and merges creates difficulties while debugging and checking the history. So, rebase is used to clear this ambiguity.   
3- I changed the same variable to create conflict intentionally. During merging, I had to resolve this conflict and picked the main branchs' variable name.
